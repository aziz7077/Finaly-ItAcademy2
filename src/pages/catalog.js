import Catalog from '@/components/CatalogPage/Catalog';
import React from 'react';

const catalog = () => {
    return (
        <div>
            <Catalog/>
        </div>
    );
};

export default catalog;